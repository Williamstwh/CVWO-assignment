<h1>Your Todo list</h1>

<%= form_with( url: my_todos_path, method: :get, local: true) do |form| %>

    <table class="searchbar">
        <thead>
        </thead>
        <tbody class="searchbar">
            <tr class="searchbar">
                <td></td>
                <td class="searchbar"><%= form.text_field :search, placeholder: "Search by title" %></td>
                <td class-"searchbar"><%= form.submit "search" %></td>
            </tr>
            <tr>
                <td class="searchbar"><%= form.check_box :includeRange %></td>
                <td class="searchbar"><%= form.datetime_select :startRange %></td>
                <td class="searchbar"><%= form.datetime_select :endRange %></td>
            </tr>
        </tbody>
    </table>
    <% console %>
<% end %>

<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Deadline</th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        <% @todos.each do |todoitem| %>
            <tr>
                <td><%= todoitem.title %></td>
                <td><%= todoitem.description %></td>
                <td><%= todoitem.due.to_formatted_s(:long_ordinal) %></td>
                <td><%= link_to 'Show', my_todo_path(todoitem) %></td>
                <td><%= link_to 'Edit', edit_my_todo_path(todoitem) %></td>
                <td><%= link_to 'Delete', my_todo_path(todoitem), method: :delete, data: { confirm: 'Are you sure?'} %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'New Todo item', new_my_todo_path %>

<script></script>
